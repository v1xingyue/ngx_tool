generate.config
=========

Get request method:			r->method		
Judge is request valid:		(r->method & (NGX_HTTP_GET|NGX_HTTP_HEAD|NGX_HTTP_POST))
Return not valid request:	return NGX_HTTP_NOT_ALLOWED
Do Nothing	:				return NGX_DECLINED

r->uri				uri infomation.		
r->uri.len			length of the uri infomation.
